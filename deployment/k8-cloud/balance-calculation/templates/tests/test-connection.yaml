apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "balance-calculation.fullname" . }}-test-connection"
  labels:
    {{- include "balance-calculation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "balance-calculation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
